export function getGooglePlayLink(id: string): string {
  return `https://play.google.com/store/apps/details?id=${id}`;
}

export function getAppStoreLink(id: string): string {
  return `https://apps.apple.com/kr/app/id${id}`;
}