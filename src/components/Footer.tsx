import styled from "styled-components";

function Footer() {
    return (
        <FooterWrapper>
            Footer
        </FooterWrapper>
    );
}

const FooterWrapper = styled.div`
    display: flex;
`

export default Footer;
